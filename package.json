{
  "name": "course-eval",
  "version": "1.0.0",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@azure/cosmos": "^4.9.0",
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
